[project]
name = "design-styling-mcp-server"
version = "0.1.0"
description = "Demo MCP server that returns UI components as HTML, CSS, and JavaScript bundles."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
]

[project.scripts]
design-mcp = "main:main"

[tool.uv]
package = false