[project]
name = "first-mcp-server"
version = "0.1.0"
description = "Minimal FastMCP server exposing Rabobank banking demo tools over HTTP."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.2",
]
