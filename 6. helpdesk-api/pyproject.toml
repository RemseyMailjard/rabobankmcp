[project]
name = "helpdesk-api"
version = "1.0.0"
description = "Dummy IT Helpdesk REST API for the MCP Server project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "mcp[cli]>=1.6.0",
    "httpx>=0.27.0",
]
